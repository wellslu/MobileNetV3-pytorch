trainer:
  name: Trainer
  num_epochs: 100

dataset:
  name: Cat_Dog
  root: data
  batch_size: 32

model:
  name: MobileNetV3

optimizer:
  name: Adam
  lr: 1.e-3

scheduler:
  name: StepLR
  step_size: 10
